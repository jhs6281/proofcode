"""ProofCode version values.

APP_VERSION changes when ProofCode features change.
PROTOCOL_VERSION changes only when the Python ↔ TypeScript JSON contract changes.
"""

APP_VERSION = "0.7.1"
PROTOCOL_VERSION = "1"
BASELINE_SCHEMA_VERSION = "1"
